An AI API project which when given a directory, can read and alter files to fit according to the users request.
  This will need a .env file with GEMINI_API_KEY = "your_key"

To Use, run main and follow it up with a prompt ex:
  uv run main.py "can you change the contents of lorem.txt to just hi"

  
